import 'package:flutter/material.dart';

const kDefaultPadding = EdgeInsets.all(20);
const kDefaultVerticalPadding = EdgeInsets.symmetric(vertical: 20);
const kDefaultHorizontalPadding = EdgeInsets.symmetric(horizontal: 20);
final kFieldBorderRadius = BorderRadius.circular(10);
final kButtonBorderRadius = BorderRadius.circular(10);
